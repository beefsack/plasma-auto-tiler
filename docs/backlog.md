# Backlog

Only meaningful pending or active work is listed.

- P1 | Rust-engine/direct-geometry migration | Rust owns topology, ordered N-ary
  groups/shares, logical workspaces/outputs, focus/navigation, movement, and
  reconciliation; thin adapters own native boundaries. KWin direct geometry is
  the actuator and Custom Tiles are legacy-only, never dual structural
  authority. Preserve production behavior through small opt-in/shadow slices.
    Checkpoint, Rust-core stabilization, static slice 3 implementation, and the
    standalone advisory entry/builder/loader prerequisite are complete. The
    namespaced standalone path deterministically binds its exact source, input,
    and IIFE bundle, and can coexist with the loaded production plugin only
    because its checked route invokes bounded read-only advisory queries with no
    topology authority or actuation. The thin static KWin-native observation
    prerequisite is complete: lexical `workspace` access and direct public
    Window APIs capture only the current active-output/current-desktop exact
    three-window scope, validate native geometry/work area and exclusions,
    serialize only opaque v1 primitives, and reobserve before pinned dispatch.
    It has no signals, mutation, controller, shortcut, or production authority.
    The unsafe interactive `queryWindowInfo` host-baseline dependency is
    removed. The exact standalone script now takes the first lexical-workspace
    exact-three snapshot immediately on run, revalidates before its pinned
    request, then emits a bounded opaque correlated after-equality verdict.
    The loader requires that verdict after the correlated result, refuses drift
    or non-success result pairings with exact-ID cleanup and no receipt, and
    preserves production-plugin continuity. Focused observer/entry/builder/
    loader tests cover snapshot drift, result correlation, stale refusal,
    source absence of `queryWindowInfo`, and partial cleanup. One authorized
    host preflight created one fresh project runtime directory before the
    failed identity check but did not retain its exact generated identity, so
    that directory is ambiguous residue and must not be broadly discovered,
    identified, inspected, or cleaned. The user explicitly preserves it
    untouched and reinstated standing authorization after the resource-order
    correction. The corrected loader now completes source/tool validation and
    full KWin owner/PID/start-tick/executable identity through the approved
    systemd/direct-parent fallback, production-loaded, advisory-absence, and
    Planner-absence checks before deterministic rebuild scratch space or any
     lifecycle resource. The current host matches the already approved exact
     direct-parent route: owner PPid equals the unit MainPID, with stable
     owner/PID/tick/boot, immutable wrapper-pair identity, and unreadable owner
     executable tolerated only under that compound proof. The repair makes the
     direct-parent selector a fixed internal status, not an error-string parser
     or caller-controlled value, and removes helper temporary files so the
     preflight is genuinely resource-free. A bounded raw Planner
     `NameHasOwner` read returned the required exact false value, so the earlier
     diagnostic was not reproduced and no new identity topology is claimed.
     Focused adversarial static coverage passes. No lifecycle ran: the current
     loader requires Planner absence through `start`, but the KWin advisory
     call requires the pinned Planner owner, and no reviewed sequencer provides
     the required absence-to-owner transition, PID-filtered diagnostic capture,
     or deterministic owner-loss timing. No planner, advisory Script, runtime
     directory, artifact, receipt, or service was created. The remaining gate
      is a reviewed fail-closed transport sequencer, then one bounded journey
      with success, stale request/revision/generation refusal, service-loss
      refusal, zero-mutation, production continuity, and exact restoration
      evidence. The sequencer and static fault matrix are complete. Two early
      live attempts stopped before effective lifecycle on omitted user-bus
      scoping and Script-object introspection scoping, each followed by a
      reviewed causal correction and public exact restoration. The final
       bounded attempt loaded and ran only the advisory Script, then stopped
       fail-closed at `advisory-invalid-input`. A reviewed redacted observer
       diagnostic split capture rejection classes and a second bounded attempt
       identified `identity-invalid`; current public `String(Window.internalId)`
       is the documented single-braced `QUuid` form, which the observer had
       rejected despite accepting the equivalent bare opaque UUID. The adapter
       now normalizes only that exact canonical form to the existing bare opaque
       schema, keeps duplicate identity refusal distinct, and logs only bounded
       category/detail tokens. The correction accepted the exact-three host
       snapshot and reached Planner dispatch with after-equality true, proving
       no extra/ineligible host-window condition. The current test-bound flat
       topology still does not truthfully represent the observed nested
       `H[A,V[B,C]]` arrangement and needs a separate explicit-boundary
       decision. The attempt then refused `advisory-timeout`: the Planner authentication gate requires a
       readable `/proc/<KWin PID>/exe`, while this host has only the separately
       approved loader direct-parent fallback. KWin, Planner/advisory absence,
       generated-output absence, production-plugin continuity, and config hash/
       mtime matched exactly after each attempt; no native window, focus,
       geometry, workspace, output, or configuration mutation occurred, and no
       runtime residue was searched or changed. No native mutation API exists on
       this path, but success/stale/service-loss and after-snapshot equality are
       not established. Do not retry: extending Rust Planner authentication to
       the unreadable-executable direct-parent identity is a pending durable
       security-boundary decision. The static
    planner-service route replaces
    `EvaluatePoc3` with bounded `DescribeAdvisoryPlan` and pins the planner unique
    D-Bus owner; public KWin scripting cannot attest the initially resolved
    same-UID service binary.
    The completed POC is archived: manual/visual evidence
  supports bounded three-window projection, focus, structural movement, resize,
  and drag gesture separation only - not production, parity, lifecycle,
  atomicity, or measured latency |
  [change](changes/rust-engine-direct-geometry-migration.md)
- P1 | static complete, residual manual/live acceptance; exhaustive harness
  recovery deferred | Retain stable Custom Tile runtime behavior, including
  drag/reflow and the existing float, fullscreen, maximize, and workspace
  paths |
  [change](changes/custom-tile-runtime.md)
- P2 | archived POC2 transport reference; superseded by the P1 migration gate |
   baseline/restoration gate passed exactly, then one POC2 planner/probe slice
   loaded, ran, and cleaned up but logged only `planner-shadow-probe-ready`; a
   focused static correction now binds its manual trigger through the KWin
   script global when `globalThis` is absent; one separately authorized
   corrected-probe slice then stopped before lifecycle because current read-only
   preflight found stale persisted tiling state, project shortcut drift, and
   retained prior diagnostic artifacts |
   No authenticated `EvaluateMove` round trip, eligible `H[A,B]` topology,
   correlation/generation/revision/precondition revalidation, or
   `could-execute`/fail-closed advisory result is established. No restoration
   claim applies because this slice made no lifecycle mutation. Do not repeat
   this stopped slice. A separate authorization is required for any new bounded
   POC2 live slice after its current preflight blockers are independently
   resolved. Preserve the
  accepted bounded N-ary Custom Tile adapter and its fail-closed unsupported
  moves; no native actuation, tray coupling, automatic delivery, KWin runtime
  replacement, stock-KWin parity claim, or Windows/macOS delivery selection
  is accepted |
   [archived decision package](changes/archive/full-cosmic-parity-architecture.md)
- P2 | archived POC3/nested harness history; no further nested recovery work |
    supervisor/KWin/bus/socket isolation and host KWin exclusion. Its one direct
    disposable-client launch atomically recorded three final `weston-terminal`
    identity triples, but immediate independent validation found all three
    recorded client PIDs absent. No accepted checkpoint has exactly three live
    clients. One later manifest-bound c1 cleanup attempt stopped the exact
    supervisor/KWin, then failed closed on the supervisor's vanished `/proc`
    identity; independent verification found no c1 runtime process, private bus,
    or nested socket. Its stale workdir remains preserved. No planner, probe,
    adapter, POC3 command, IDs, owner, generation, revision, visible Rust-owned
    layout, or visual claim exists. The client-lifetime cause is unproven and
    blocks this bounded live route. b1, a4, `a1`/`a2`/`a3`, and coredumps remain
    preserved. Static readiness now includes validator-valid synthetic proc
    environments and isolated bounded disposable-client diagnostics; this does
    not establish client lifetime or change the preserved-residue status |
   A later d1 fresh nested launch also passed with private isolation and host
   exclusion, but its one corrected client launch recorded no identities: each
   stderr capture reports `File too large` creating a `1582984 B` private-runtime
   buffer. Static evidence identifies inherited `RLIMIT_FSIZE=32768`, not storage,
   as the cause; the capture now uses a 32 KiB FIFO drainer with no target file
   limit, exact drainer/holder cleanup, and synthetic regression coverage. This
   static correction has no live confirmation. Independent validation found no
   live client or POC3 group. The sole d1 exact cleanup stopped nested KWin and
   its private supervisor, then failed closed because the supervisor identity was
   stale at the post-stop check; d1 is preserved inert residue with no private
   bus/socket or live target process |
     The authorized e1 slice stopped at the launch freshness guard before any
     nested process or client: the sole launch exited 1 because the pre-created
     workdir was not fresh. `nested-kwin-spike.sh check` passed for pinned 6.7.4
     KWin, but no manifest, client, diagnostic, stability, cleanup, host baseline,
     or isolation evidence exists. e1 contains bounded worker evidence only and
     is preserved. Do not retry e1 or perform cleanup from its no-manifest
     checkpoint |
     A later fresh high-entropy workdir `/tmp/opencode/nested-63d1ea5016b28879`
     passed one nested check, one launch, and one manifest validation. The sole
     corrected client launch recorded three `weston-terminal` PID/start-tick
     identities, but immediate manifest-bound validation found client 1 absent;
     independent review found all three client and drainer PIDs absent. No
      exact-three live non-zombie checkpoint, file-size diagnostic result, or
      stability evidence exists. One later exact manifest-bound cleanup
      invocation reconciled the already-absent clients and stopped the nested
      KWin, then failed closed because the supervisor PID was stale or unreadable
      before its exact-stop check. Independent verification found all recorded
      target PIDs, private bus, and nested socket absent; only stale workdir
      evidence remains. Host KWin was excluded and untouched. No planner, probe,
      adapter, POC3, host D-Bus, journal, or host-process action occurred. Do not
      retry or clean up this stale-manifest checkpoint; no autonomous live action
      remains authorized. Final retained-artifact
     diagnosis found no exit status, signal, stability observation, or
     compositor-side diagnostic; `weston-terminal --shell` and the retained
     cursor/`?2004` output do not establish an exit cause. No small correction
     is justified. The failure precedes and does not disprove the static
     Rust/KWin POC3 design; the current disposable-client route is
     authorization-blocked, not technically disproven. Do not add a
     project-owned client or dependencies without fresh explicit authorization |
      Static manual-client staging is now available instead: three explicit
      `/nix/store` client argv vectors occupy slots 1-3 with private `env -i`
      isolation, exact client/drainer identities, bounded diagnostics, exact
      cleanup, and a PID-bound exact-three ID probe. No default trio exists;
      locally metadata-verified `konsole --separate` remains live-unproven.
      Its authorized fresh checkpoint
      `/tmp/opencode/poc3-live-18801fbda1134dce824b931afbf9814b` passed one
      check, launch, and manifest validation, recording live private
      `dbus-run-session` `912958`/`11003334` and wrapped KWin
      `912961`/`11003334`. The sole exact slot-1 `konsole --separate` invocation
      timed out after 120 seconds when its final executable was
      `.konsole-wrapped`, not the requested `konsole`; it recorded zero manual
      identities. Slots 2-3 and `ready` did not run, and workdir-local stderr
      and nested logs are empty. Exact-three process/window evidence is absent.
      The sole permitted cleanup validation failed before cleanup because `jq`
      was not an exact Nix-store executable, so this live nested residue is
      preserved. No planner, probe, adapter, POC3 command, host bus/journal,
      host process enumeration, or KWin actuation ran. Exact-PID inspection
      found private runtime/bus/socket bindings and no host kwinrc or bus path
      in nested command line/maps/known FDs, but generic host GTK/Starship
      config paths remain in its environment, so strict checkpoint isolation is
      not cleanly established. Static-only correction now binds only exact
      `konsole` launcher-to-same-output-wrapper identities, pins security
      helpers including profile-linked `jq` to validated Nix-store executables,
      and clears/rejects the observed GTK/Starship variables. The existing
      running checkpoint cannot be retroactively sanitized and now fails exact
       validation, so it was not cleanup-eligible under the tightened rule. A
       later explicitly authorized identity-bound cleanup waived only that
       GTK/Starship gate: exact revalidation passed for supervisor
       `912958`/`11003334` and wrapped KWin `912961`/`11003334`, then TERM to
       KWin exited it within the bounded wait and the supervisor exited without
       a signal. Independent verification found both former PIDs, the private
       bus, and nested socket absent; the workdir and its evidence are
       preserved. Host KWin `2324`/`2590` and `2329`/`2592` remained distinct
       and untouched. No POC3 action or host mutation occurred |
      A fresh high-entropy manual-Konsole checkpoint
      `/tmp/opencode/poc3-manual-stage-bb8b0e54ee3506ed9d707a88f6a08099`
      passed one nested check/launch/manifest validation with private
      wrapper-aware identities, private XDG/KDE/bus/socket state, and
      checkpoint host-leakage rejection. One exact-store `konsole --separate`
      launch per slot recorded three final `.konsole-wrapped` PID/start-tick
      identities, but sole `ready` and independent exact-PID review found all
      clients/drainers absent. No eligibility probe, planner, POC3, adapter,
      geometry, or focus action ran; empty bounded logs do not establish an
      exit cause. The sole authorized manifest-bound cleanup reconciled the
      gone manual identities and stopped the nested chain, then failed closed
      on an absent post-stop supervisor identity. Both recorded PIDs and the
      private bus/socket are now absent and the workdir is preserved. No retry
       or host mutation/restoration occurred; diagnostic host-`kwinrc` drift is
       unattributed |
       Static-only diagnostic client route (no live nested attempt authorized
       or performed): additive manually launched diagnostic/test-only raw
       xdg-shell binary `poc3-diagnostic-client` with explicit private
       runtime/socket or bounded manifest CLI, slots 1-3 with distinct
       markers, required protocol/SHM handshake, and bounded redacted
       diagnostics. `nested-manual-clients.sh launch-diag` uses the exact
       binary/private-env route with no Konsole fallback and exact cleanup.
       Added `memmap2 0.9`, `wayland-client 0.31` no defaults,
       `wayland-protocols 0.32` client no defaults, existing `rustix` `mm`;
        no `devenv.nix` change/system dependency/restart gate. Current full
         `cargo test`, diagnostic Clippy, manual/cleanup fixture suites
        (250/0 and 144/0), shell syntax, and diff check pass; full-target
        Clippy remains blocked only by pre-existing unmodified `src/tray.rs`
        lints. Independent review corrected configure ordering, diagnostic
        exclusive creation, global versions/event loop, terminal/signal
         integrity, and cleanup snapshot validation. Final live proof remains
         pending authorization; disposable tooling only, no product architecture
         claim |
       The later diagnostic-client slice visibly mapped only the surviving
       slot-2 raw xdg-shell/SHM client (green square, white border, two white
       top-left squares); no geometry, focus, tile, or POC3 behavior is
       accepted. Exact slot-2 and nested-chain cleanup completed with host KWin
       excluded; stale absent slot 1 was not signalled and the workdir/logs
       remain preserved. Retained diagnostics cannot attribute slot-1's silent
       exit, and synthetic sequential-slot coverage shows no launcher kill, so
       no correction is justified. A later exact-three diagnostic stage at
       `/tmp/opencode/poc3-diag-stage-20260905-9f8c20b1d7e46a3c5b0e17fd94c26a81`
       passed one nested check/launch/validation and one `launch-diag` call for
       each slot. All three diagnostic captures reached `first-map 320x240`
       without recorded close/protocol errors, but all exact client and drainer
        identities were absent before sole readiness/validation. No ID probe,
        planner, adapter, POC3, geometry, or focus action ran; KWin window count
        is unestablished. The sole exact cleanup reconciled the absent manual
        group, then failed closed on a vanished supervisor identity. All recorded
        runtime PIDs, private bus, and nested socket are absent; the workdir/logs
        remain preserved. No client or launcher cause is established |
       A fresh resident diagnostic-trio workdir
       `/tmp/opencode/poc3-live-618c8578-0ecb-4764-97d0-7989eb5911eb` passed
       one nested check/launch/validation. One trio launch reached `READY` with
       direct client PIDs `2289401`, `2289402`, `2289403` and supervisor
       `2289393`; manifest-bound client readiness passed. The sole read-only ID
       probe stopped before loading because its `jq` resolver rejected
       `/etc/profiles/per-user/beefsack/bin/jq` as not an exact Nix-store
       executable. No planner, probe script, adapter, POC3 command, IDs,
       geometry, or focus action ran. One exact cleanup invocation then failed
       closed on stale or unreadable private supervisor `2287178` after its
       diagnostic-only host-`kwinrc` drift warning; the workdir and residue are
       preserved with no retry. Static-only tool-authority correction with no
       live action and no residue change: probe/planner mutation tools pinned
       to validated store executables, planner PATH discovery removed
       (explicit `PLANNER_BIN` plus fixed repo targets kept, identity binding
       kept), readlink canonicalizer requires basename plus regular store
       final, multicall no-overclaim rule, no bare fallbacks; canonical
       185/0, probe 124/0, planner 126/0, spike 206/0, cleanup 144/0,
       disposable 443/0, manual 276/0, command-nested 28/0, diff check clean.
        Test-hook/repo-root/startup-path observations are same-caller
        non-boundaries, stated in the change record |
        A later fresh resident diagnostic-trio stage at
        `/tmp/opencode/poc3-live-ab188c80042232ce631ef6cbdf960de8` passed one
        check/launch/manifest validation and had three live first-mapped `320x240`
        diagnostic clients, slot 1 `3905879`, slot 2 `3905880`, and slot 3
        `3905881`, with private isolation and host exclusion independently
        verified. The sole pinned-store-jq read-only ID probe failed before bundle
        load because its supervisor-stat awk guard was malformed. Static-only
        repair replaced it with the manifest-compatible fail-closed Bash parser
        and added focused proc-stat, exact executable, PID-reuse, quoting, and
        partial-record regressions; probe `155/0`, canonical `185/0`, launcher/
        manifest `206/0`, cleanup `144/0`, shell syntax, and diff check pass.
         No live retry or staging-path modification occurred. No script ID, opaque
         IDs, eligibility, or scope proof exists. The one exact cleanup stopped all
         recorded processes but failed closed at a vanished post-stop supervisor
         identity; its workdir/raw probe diagnostics and all prior residue remain
         preserved. Do not retry probe or cleanup. Static script-ID correction
         confirms KWin 6.7.4 `-1` is failure and returned `0` is valid
         `/Scripting/Script0`; lifecycle routes dynamically bind it, and focused
         regressions cover zero, failures, malformed replies, duplicate guards,
         and partial cleanup. Probe completion now accepts only bounded post-run
         output with an exact successful owner/generation/nonce/revision/three-ID
         payload, rejecting stale same-nonce and error markers. No autonomous live
         action is authorized; after separate authorization, the next slice is one
         fresh diagnostic-trio stage and exactly one canonical-jq read-only ID
         probe, with no planner or POC3 actuation |
        A completed terminal authorized slice at
        `/tmp/opencode/staging1-4edd96cbf22d8443f90ccde719863827` built
        current diag binaries and passed check, one launch, manifest
        validation, one resident trio launch, and manual ready/validation.
        Private `dbus-run-session` `1061838`/`16232457`, KWin
        `1061841`/`16232457`, supervisor `1064132`/`16233290`, slots
        `1064140`/`1064141`/`1064142`/`16233300`; three `first-map 320x240`
        and red/green/blue source mappings with app IDs
        `org.plasma-auto-tiler.poc3-diag-1/2/3` verified. The sole canonical
        store-jq probe (owner `live-unit2-worker`, `gen-20260906-w2`,
        `probe-20260906-w2a`) returned partial script ID `0` with no bounded
        post-run success marker/payload; no Script0 introspection or
        ID/eligibility/output/workspace success is asserted. Plugin
        self-unloaded with no retained record and absent Script0; exact probe
        cleanup correctly refused the unrecorded ID. Independent validation
        passed checkpoint/isolation and unloaded plugins; one full exact
        cleanup stopped all six private PIDs, removed bus/socket, then failed
        closed on stale supervisor `1061838`. Workdir/evidence preserved; no
        retry, planner, adapter, POC3, production, host, or config action.
        Static persistent direct-enrollment pivot complete: external
        probe/log-marker route stopped as start dependency after two valid
        Script0/self-unload runs lacked a completion marker
        (diagnostics/static coverage only); manifest-bound persistent direct
        start (exact PID/tick/app-id/slot/color, owner/generation/nonce,
        private bus plus exact Planner unique owner; internal exact-three IDs,
        no captions/guessed IDs; Rust H[A,V[B,C]] geometry/focus, sequential
        revalidated apply, typed completion/status plus post-observation, stays
        loaded, exact Script ID including 0; exact-unload plus trio-only
        cleanup) is static-only initial-start-only with no
        production/startup/host/Custom Tile/shortcuts/persistence/tray/KCM
        changes. No autonomous action remains; one final fresh private nested
        live start journey only under explicit authorizations and guide (direct
        start, no probe, then manifest-bound persistent cleanup); later no-ID
        focus/move routing follows separately. Static-only planner
        caller-environment correction (2026-09-06, no live action): prior
        terminal staging planner launch refused the host caller XDG runtime
        before child construction; the launcher now accepts normal hostile
        host caller state while deriving the planner child/service bus,
        runtime, XDG/KDE, exact nested KWin authorization, and private D-Bus
        ownership from the validated manifest; host-valued/tampered/missing
        manifest values and production-reachable test/validator/proc/env
        authority are rejected; planner 171/0, canonical 185/0, nested
        spike/manifest 206/0, clients 443/0, cleanup 144/0, relevant POC3
        suites, Rust planner 28/0, full cargo test 308/0 from prior
        verification, format/diff check clean, clippy retains only unrelated
        src/tray warnings; no live retry/action occurred and
        residues/coredumps remain preserved; this correction does not
        authorize a live retry. Terminal diagnosis (no live action):
         latest retained planner-launch artifacts are absent, so local
          root cause is indeterminate; architecture remains
          offline-proven/live-unproven, and no further nested POC3 live attempt
          is justified in this unit. The separately authorized pragmatic host
          pilot is statically complete: it pins KWin, production plugin/source,
          a diagnostic trio, planner, persistent no-ID adapter, exact cleanup,
          and explicit resume without config, dotfiles, rebuild, shortcut, or
          session-boundary changes. Its accepted limitation is operational
          binding only - KWin 6.7.4 cannot prove Script-object attribution,
          duplicate count, handler absence, or exact running-state restoration.
           The initial read-only `baseline` attempt stopped fail-closed because
           KWin PID `3568836` had no readable canonical `/proc/.../exe` identity.
           No suspend, clients, planner, adapter, geometry/focus, cleanup, or
           production reload ran. A later bounded read-only diagnosis observed
           stable owner `:1.9`, PID `3568836`, and start tick `13991576` before
           and after exact-PID proc reads; only canonical exe readlink was denied.
            Existing fail-closed baseline behavior is correct. Focused synthetic
            identity regressions and host baseline/suspend/pilot suites pass.
            The explicitly authorized fallback compounds the D-Bus KWin unique
            owner/PID, `/proc` tick and boot ID,
            `plasma-kwin_wayland.service` Type=dbus/BusName=org.kde.KWinWrapper
            state/MainPID, and Nix-store ExecStart identity only when /proc exe
            is unreadable; readable `/proc/exe` must agree. The single
            `bash scripts/poc3-host-pilot.sh baseline` attempt failed closed,
            without a receipt, with
            `error: unit MainPID 3568829 does not match KWin PID 3568836` and
            `error: KWin PID 3568836 executable identity is unreadable and systemd fallback failed`.
             This establishes an unresolved mismatch: the `org.kde.KWin` D-Bus
             owner PID does not equal the service MainPID. No suspension,
             client/planner/adapter launch, geometry/focus, cleanup, resume, or
             host state mutation occurred. An authorized relationship check
             found direct parent-child ancestry and stable owner/PID/tick/boot
             evidence, but the owner cgroup was `/` rather than the service
             control group. Same-unit containment remains unproven. This
             supersedes the prior unimplemented status: the direct-parent
             fallback slice is complete with authorized scope direct D-Bus owner
             PPid exactly equals `plasma-kwin_wayland.service` MainPID, no
             deeper/arbitrary descendants; owner cgroup `/` accepted only in
             direct-parent host-pilot mode and not proving same-unit
             containment; owner readable exe mismatch fails closed. Static
             evidence before the live checkpoint was direct-parent 35/0,
             identity-compound 24/0, identity 49/0, baseline 75/0, suspend
             102/0, pilot 115/0, trio 86/0, planner 83/0, command 79/0;
             aggregate 648/0, 16 syntax; diff check passed. The slice's sole
             `bash scripts/poc3-host-pilot.sh baseline` ran exactly once,
             failed with no baseline, with
             `error: unit MainPID 3568829 does not match KWin PID 3568836` then
             `scripts/poc3-host-kwin-identity.sh: line 135: rest: unbound variable`;
             exact exit code unavailable. No retry, suspend/resume, clients,
             planner, adapter, geometry/focus, cleanup, configuration, or host
              mutation occurred. Suspend is not ready; earliest next checkpoint
              requires user direction because the single baseline is spent and
              the `rest` nounset failure is a code defect requiring a separate
              authorized correction and a new baseline authorization. One later
              authorized suspend invocation succeeded with retained receipt
              `/run/user/1000/plasma-auto-tiler-host-pilot/suspend-receipt.json`:
              exact production `plasma-auto-tiler-kwin` changed only from
              `loaded` to `not-loaded` after a true unload reply; owner `:1.9`,
              PID `3568836`, tick `13991576`, direct-parent MainPID `3568829`,
              boot/unit, and immutable wrapper pair remained exact. Independent
              verification found no trio/planner/persistent/resume receipt,
              planner owner, or checked POC3 KWin script, and repository status was unchanged;
              the runtime-only tool supplies no-persistent-mutation evidence.
               It also found stale nested diagnostic-client processes in prior
               `/tmp` workdirs, so the required no-diagnostic-process checkpoint
               failed. Production remains suspended for user observation. No
               cleanup, clients, planner, adapter, geometry/focus, resume, or
               further host action is authorized. On 2026-09-07, focused
               receipt-scoped host-trio guard and compound-KWin identity repairs
               passed host-trio `138/0`, pilot `120/0`, baseline `88/0`, and
               suspend `113/0` harnesses plus syntax/diff checks. Read-only host
               observations selected `eDP-1` and desktop
               `f18245bc-0b73-4cda-9647-091f85aab333`; the suspend receipt,
               KWin identity, production-not-loaded word, and absent Planner
               owner remained valid. One pre-spawn launch exposed obsolete trio
               KWin helper record widths; the causal repair was statically
               covered. Two later direct trio launches stopped before receipt
               creation at supervisor identity convergence. Exact inspection
               found only baseline/suspend records and no current built
               diagnostic/supervisor PID, trio/planner/persistent/resume record,
                or Planner owner. No signals, cleanup, planner, adapter,
                geometry/focus, workspace/output, shortcut, Custom Tile, config,
                or production reload occurred; production remains suspended.
                Final host-trio checkpoint only: staging succeeded with receipt
                `/run/user/1000/plasma-auto-tiler-host-pilot/host-trio-receipt.json`,
                schema `poc3-host-trio-v1`, workspace `f18245bc-0b73-4cda-9647-091f85aab333`,
                output `eDP-1`, alias `scope-1`, host `/run/user/1000`/`wayland-0`;
                supervisor `3592922`/`23352227`, clients `3592938`/`23352240`,
                `3592939`/`23352240`, `3592940`/`23352240`, app IDs
                `org.plasma-auto-tiler.poc3-diag-1/2/3`; read-only trio validate
                reported manifest valid with three slots, production isScriptLoaded
                false, Planner owner absent; static Rust diag supervisor `12/0`,
                host trio `5/0`, host trio script `173/0`, syntax/format/diff check
                passed; prior missing client `--host`/host path mismatch repaired,
                then Nix `env` delimiter after assignments emitted exact
                `env: '--': No such file or directory` and was moved directly after
                `-i` with static coverage. No planner, adapter, focus, move, layout,
                cleanup, resume, config, shortcut, workspace/output, or Custom Tile
                action occurred; production remains suspended under its existing
                valid receipt |
         [archived change](changes/archive/poc3-disposable-rust-actuation.md)
- P2 | archived POC reference | Pointer evidence and its harness failures are
  retained as migration reference. Final cleanup unloaded
  `pat-pointer-reconcile`, `pat-reconcile-reset-20260907`, and verified absent
  `pat-pointer-debug-20260907`, `pat-pointer-resize-20260907-r571321`,
  `pat-live-resize-20260907-p554898`, and `pat-live-poc-20260907`; production
  is resumed |
  [record](changes/archive/pointer-interactive-resize.md)
- P1 | static complete, pending manual visual smoke | Drag one tiled window over
  an occupied target leaf, confirm the whole-leaf rectangle preview, release,
  and visually confirm nested split placement; do not attempt stale-harness
  recovery | [change](changes/archive/nested-placement-affordance.md)
- P1 | static complete; current-session user-local dogfood attempt made no
  mutation and stopped fail-closed because authoritative current-session
  discovery evidence was unavailable; no live identity/config/plugin/PID/proc
  claim is accepted | Accept the active-window border and Desktop Effects KCM
  using a consumer-host-pkgs Nix build that explicitly supplies and verifies
  the `kwin` package matching the running KWin plus its `kwin.dev`;
  `UseThemeColor` (default true, migration-free) is implemented with native
  KCM control and existing hot-apply/repaint coverage, static/native/Nix
  package evidence passed with no live Plasma action; the verified
  output handoff to the documented user-local dogfood root remains pending
   with preimage/rollback checks; the user-owned manual visual border/KCM check,
   including width hot-apply and theme-override behavior, is accepted after a
   rebuild/new session; runtime/config/reload/restoration gates remain future
   gates; external NixOS/Home Manager activation remains outside this change |
  [decision](decisions.md#native-active-border)
  [change](changes/archive/active-border-colour-override.md)
- P1 | static complete, pending live/release | Manual tray visual/Settings-click
  is accepted after rebuild/new session; residual gates are KWin-origin SNI
  authority, watcher ordering, native ABI load, real install/packaging,
  login/autostart, and update/rollback generation |
  [change](changes/archive/tray-carrier.md)
- P1 | Nix-only script resolution/session activation accepted; pending external
  current-host live | Consume the Nix flake from an external NixOS/Home Manager
  configuration, then validate clean install, update, and Nix generation
  rollback. The user-owned restart resolved only the active-generation store
  script with the local shadow absent; it does not accept Custom Tile behavior,
  physical behavior, tray/effect behavior, or loaded-memory byte identity.
  Static outputs, immutable managed tray startup, ownership, source filtering,
  and lockfile evidence are accepted |
  [change](changes/archive/nix-current-host-delivery.md)

- P1 | static complete, pending user-run live Apply/Revert/interrupted-recovery
  | Explicit KCM shortcut override with recovery (`Meta+L`/`Meta+Esc`) |
  [change](changes/shortcut-override.md)
- P2 | parked | Retain the floor-ratio fallback unless a qualifying isolated
  nested KWin proof establishes a safe improvement | [change](changes/floor-ratio-feasibility.md)
- P2 | parked | Establish a safe Integrated Plasma structural feasibility
  verdict; the unsafe nested path remains stopped | [change](changes/integrated-plasma-structural-feasibility.md)
- P2 | parked | Complete sustained JavaScript workload evidence before choosing
  a native replacement for discrete window management | [change](changes/js-workload.md)
- P2 | parked | Prove multi-window Custom Tile stability before choosing true
  grouped or tabbed window behavior | [change](changes/grouped-windows.md)
- P2 | parked | Resolve complete keyboard-layout support after initial release |
  [change](changes/shortcuts.md)
- P2 | parked | Verify trailing-empty workspace anti-oscillation on a multi-output
  machine | [runbook](live-oscillation-verification.md)
- P3 | parked | Validate bspwm, Hyprland, and COSMIC behavior at their actual
  runtimes | [comparison](reference-wm-comparison.md)
- P3 | parked | Publish the reproducible KPackage artifact to KDE Store and
  GitHub Release after MVP delivery dependencies are complete | [delivered foundations](changes/archive/delivered-foundations.md)
