# Log: Integrated Plasma Structural Feasibility

Append-only. Append after a meaningful checkpoint: an accepted semantic unit,
verified partial result, blocker, pending user decision, unsuccessful host
attempt, context handover, semantic or governance change, independent review
finding, commit, or approved plan change.

## 2026-08-10 (creation and approval)

- Role / unit: Lead / change creation / no dispatch.
- Result: created the user-approved Expanded structural-feasibility gate from
  the archived integrated-tiling-workspace-value verdict. Unit-01 is active;
  no source research, live interaction, installation, configuration, package
  change, test window, or proof execution occurred.
- Files / commit: `spec.md`, `plan.md`, `state.md`, and `log.md`; no commit.
- Verification: the approved workflow, architecture-neutral outcome rule,
  fail-fast gate, source-only opening units, fresh live-authorization checkpoint,
  and sustained-workload preservation boundary are recorded.
- Notes / blocker: unit-04 remains prohibited pending unit-03 and fresh user
  authorization. The next dispatch is `unit-01/attempt-01` only.

## 2026-08-10 (unit-01 accepted)

- Role / unit: Lead / `unit-01/attempt-01`, one source-only Worker attempt.
- Result: accepted the KWin 6.7.3 structural API-surface matrix. The pinned
  source establishes one documented supported root lookup, version-coupled
  structural routes, source-only runtime uncertainties, and no supported
  separate-process panel interface. Every mandatory capability retains a
  bounded route, so the fail-fast rule does not park unit-02. A scripting-only
  proof is not recommended; the continuing path is version-coupled feasibility.
- Files / commit: added `research/kwin-api-surface.md`; updated `plan.md`,
  `state.md`, and `log.md`; no commit.
- Verification: source tag `v6.7.3`, commit
  `45ec9a6d0ed312a803ff5658a2a3e61f221566c6`, was clean and detached. The
  sparse checkout omitted `src/tiles/` from the working tree but the pinned
  tracked blobs were verified and read with `git show`; full binding-surface
  searches used `git grep HEAD`. Lead inspected the matrix and representative
  Workspace, Tile, QML-registration, and non-invokable window-assignment
  evidence. No runtime or live-session interaction occurred.
- Notes / blocker: capabilities 4, 5, and 6 require later authorized runtime
  validation; capability 10 needs a bounded bridge because no supported
  separate-process tile interface exists. Next dispatch is
  `unit-02/attempt-01` only.

## 2026-08-10 (unit-02 accepted)

- Role / unit: Lead / `unit-02/attempt-01`, one source-only Worker attempt and
  one same-scope evidence correction round.
- Result: accepted the package-composition boundary. The smallest reversible
  proof carrier is one JavaScript `KWin/Script` KPackage driving the
  version-coupled Custom Tile surface; this is a unit-03 proof recommendation,
  not a production architecture or language selection. No panel, IPC, effect,
  or native component is needed for the approved workflow. The shipped Pager
  satisfies workspace indication without structural-tree state; a structural
  panel indication remains optional and would need a bounded bridge.
- Files / commit: added `research/package-composition.md`; updated `plan.md`,
  `state.md`, and `log.md`; no commit.
- Verification: Lead inspected the complete new-file diff, the corrected
  lifecycle attribution, the source-cited capability and composition matrices,
  and the official KWin scripting tutorial. Pinned KWin source remains
  `v6.7.3` commit `45ec9a6d0ed312a803ff5658a2a3e61f221566c6`. No live-session
  interaction, installation, package/configuration change, D-Bus call, script
  load, test window, dependency change, or sustained-workload contact occurred.
- Notes / blocker: package upgrade/removal semantics and bounded reload timing
  are unproven lifecycle items for the later authorized proof; capabilities 4,
  5, and 6 remain runtime-validation required. No package-composition fail-fast
  blocker exists. Next dispatch is `unit-03/attempt-01` only.

## 2026-08-10 (unit-03 host-unknown reconciliation)

- Role / unit: Lead / `unit-03/attempt-01`.
- Result: the Worker returned no report. This is an unsuccessful, non-resumable
  host-unknown attempt and supplies no evidence.
- Files / commit: no `research/proof-protocol.md` was created; `state.md` and
  `log.md` updated; no commit.
- Verification: Lead reconciled the untracked repository status, confirmed the
  required research file is absent, and reviewed the current checkpoint log.
  No source, package, configuration, D-Bus, KWin, terminal, or live-session
  action occurred.
- Notes / blocker: no semantic or safety blocker was discovered. Dispatch a
  fresh compressed `unit-03/attempt-02` recovery Worker; unit-04 remains
  prohibited pending the accepted protocol and fresh user authorization.

## 2026-08-10 (unit-03 safety-review correction)

- Role / unit: Lead / `unit-03/attempt-02`, independent protocol review.
- Result: review found an unauthorized read-only local tool-presence/help query
  in a source-only unit, a malformed `createDesktop` command, an ungated
  proof-collapse shortcut that could mutate a real desktop, incomplete
  persistent-state inventory for `[Desktops]`, and under-enumerated manual
  actions. The local queries made no package, configuration, D-Bus, KWin,
  terminal-control, or window mutation, but they cannot support the protocol
  and are recorded as a scope deviation.
- Files / commit: `research/proof-protocol.md` is review input; `state.md` and
  `log.md` updated; no commit.
- Verification: independent review checked cited pinned KWin source and the
  proof draft. Its documented blockers are the ungated structural shortcut and
  malformed desktop-creation command; no live proof action occurred.
- Notes / blocker: one same-scope correction round must replace local-observed
  command behavior with source-cited behavior, gate every topology mutation to
  the proof desktop, correct the command/action inventory, and inventory
  `[Desktops]` before acceptance. Unit-04 remains prohibited.

## 2026-08-10 (unit-03 protocol recovery required)

- Role / unit: Lead / `unit-03/attempt-02` correction review.
- Result: the correction resolves the independent review's command, shortcut,
  state-inventory, and action-enumeration findings, but the resulting protocol
  still installs a KPackage and writes its package configuration. Pinned KWin
  exposes `org.kde.kwin.Scripting.loadScript`/`unloadScript`, and no cited
  source makes package installation mandatory; retaining it conflicts with the
  approved smallest-carrier constraint.
- Files / commit: `research/proof-protocol.md` remains unaccepted review input;
  `state.md` and `log.md` updated; no commit.
- Verification: Lead inspected the corrected source-cited protocol, including
  the script lifecycle, state inventory, reversal commands, mutation gates,
  authorization request, and scope-compliance disclosure. No additional live
  action occurred during review.
- Notes / blocker: attempt-02 has used its permitted correction round. A fresh
  narrowed recovery must use the D-Bus load/unload path without package install
  or `Script-<id>`/`[Plugins]` mutations, and must preserve the corrected safety
  controls. Unit-04 remains prohibited.

## 2026-08-10 (unit-03 second host-unknown reconciliation)

- Role / unit: Lead / `unit-03/attempt-03`.
- Result: the fresh narrowed recovery Worker returned no report. This is a
  second unsuccessful, non-resumable host-unknown attempt and supplies no
  evidence; the existing `research/proof-protocol.md` remains the rejected
  attempt-02 review input.
- Files / commit: no new recovery output or commit; `state.md` and `log.md`
  updated.
- Verification: Lead reconciled repository status, confirmed the only protocol
  file is the already rejected attempt-02 draft, and checked the recorded
  recovery checkpoints. No additional live action occurred.
- Notes / blocker: two host-unknown attempts plus the rejected attempt-02
  protocol require Orchestrator reassessment before another Worker dispatch.
  The required narrowed route is D-Bus `loadScript`/`unloadScript` with no
  package installation or KWin script-config mutation. Unit-04 remains
  prohibited; no fresh authorization request is presented.

## 2026-08-10 (unit-03 terminal host-unknown reconciliation)

- Role / unit: Lead / `unit-03/attempt-04`.
- Result: the user-authorized fresh narrowed recovery Worker returned no report.
  This host-unknown attempt supplies no evidence and did not replace the
  rejected attempt-02 protocol draft.
- Files / commit: no recovery output or commit; `state.md` and `log.md`
  updated.
- Verification: Lead reconciled repository status, confirmed the only protocol
  file remains the rejected attempt-02 draft, and verified no additional
  live-state action is recorded.
- Notes / blocker: the user's recovery instruction requires terminal handover
  on this outcome. No further Worker or reviewer dispatch occurred. A successor
  needs Orchestrator reassessment and must restart from the mandatory D-Bus
  `loadScript`/`unloadScript` carrier without package or persistent script-
  configuration mutation. Unit-04 remains prohibited; no authorization request
  is valid.

## 2026-08-10 (unit-03 accepted: keyboard-preselection blocker resolved)

- Role / unit: Lead / `unit-03/attempt-05`; three serial Worker slices: static
  shortcut-lifecycle research, protocol revision, and independent safety review;
  one same-scope correction round.
- Result: accepted `research/proof-protocol.md` as the complete reversible
  unit-03 protocol and exact unit-04 authorization request. The previous
  shortcut blocker is resolved: `Script::registerShortcut` parents its action
  to the script; script unload destroys it and drives asynchronous KGlobalAccel
  unregister. The shipped KGlobalAccel D-Bus API supports targeted
  `unregister(component, action)` and `Component.invokeShortcut(action)`.
  The protocol registers, dispatches, and removes only a fresh sentinel action;
  it dynamically discovers its component and polls semantic action/config
  absence before and after its one-action fallback.
- Files / commit: updated `research/proof-protocol.md`, `plan.md`, `state.md`,
  and `log.md`; no commit.
- Verification: independent review checked the pinned KWin `v6.7.3` source,
  the exact installed KGlobalAccel 6.28.0 D-Bus XML/client API surface, and the
  KGlobalAccelD 6.7.3 static daemon surface. The single correction removed a
  stale no-registration claim and disclosed harness-death, physical-keypath,
  and duration limits. No live session, D-Bus, package, configuration, source,
  or dependency action occurred.
- Notes / blocker: `Component.invokeShortcut` proves daemon-to-script callback
  dispatch, not physical key-to-daemon/Wayland delivery. Harness death before
  unload can leave the sentinel shortcut registered, so recovery is outside the
  protocol and requires fresh authorization. Unit-04 remains prohibited until
  the user authorizes the accepted exact request.

## 2026-08-10 (unit-04 crash recovery reconciliation)

- Role / model / unit: Lead (`lead-openai`, `openai/gpt-5.6-terra`) /
  `unit-04/attempt-01`; host-only reconciliation, no Worker dispatched.
- Result: the interrupted runtime attempt is unsuccessful. Its durable record
  shows preflight through T3b, then `manual-t2 go-signal timeout`,
  `ABORT`, and `RECOVERY_COMPLETE`. This is not proof completion. All resulting
  runtime evidence is inadmissible for acceptance, responsiveness, cleanup, or
  the feasibility verdict. Preserve it only for inspection.
- Files found / provenance limit: present untracked proof artifacts are
  `proof/run-proof.sh` and `proof/structural-proof.js`; present untracked runtime
  artifacts are `results/run.out`, `results/assertions.log`, `results/sink.log`,
  `results/progress.log`, `results/manual-t2.ready`, `results/snapshot/`, and
  `results/evidence/`. The repository has no tracked baseline (all project
  content is untracked), so timestamps identify these as attempt-era artifacts
  but cannot establish an authoritative before/after diff. No file was deleted
  or rewritten; this log and `state.md` are the only Lead-owned changes.
- Evidence integrity: result files are partial and terminally failed, not
  oversized. `snapshot/desktops-section.txt` contains only `[Desktops]` and
  `desktops-semantic.txt` is empty, so those config snapshots are malformed for
  semantic restoration evidence. The durable D-Bus desktop snapshot is usable
  only for live residue comparison. No runtime claim from the attempt is
  accepted.
- Read-only live checks after the user's restart: KWin
  `org.freedesktop.DBus.Peer.Ping` succeeded; `isScriptLoaded` is `false` for
  `plasma-auto-tiler-structural-proof`; current desktops, current id, count,
  and rows exactly equal the pre-run D-Bus snapshot; the attempted temporary
  desktop name is absent; no attempted `[Tiling]` subgroup exists for
  `099828b1-f155-487c-8801-42340019fb64`; no
  `plasma-auto-tiler-kb-*` action is returned by read-only KGlobalAccel
  component inspection; and neither the recorded sentinel action nor proof id
  appears in read-only `kglobalshortcutsrc` inspection. The targeted sentinel
  sequence has no durable exact post-run identifier beyond the action id/prefix,
  so no broader shortcut query was used.
- Process-check limitation: the targeted `pgrep -af` sentinel-window and
  logger/capture checks each matched their own checking shell. They are
  inconclusive and do not establish absence. No follow-up process query ran
  after the required residue stop.
- Blocker / cleanup: `isScriptLoaded("krohnkite")` is `false`, but read-only
  `~/.config/kwinrc` inspection reports `krohnkiteEnabled=true` and
  `[Script-krohnkite]`; read-only `kglobalshortcutsrc` inspection additionally
  finds 35 `Krohnkite*` action entries. Krohnkite is therefore not loaded but
  remains enabled in configuration. No configuration mutation, unload, removal,
  cleanup, script start, window/desktop operation, shortcut invocation, or
  service restart was performed. Stop here. The exact minimal cleanup proposal
  is to set only the existing `[Plugins]` key `krohnkiteEnabled` to `false`.
  The five `[Script-krohnkite]` keys (`screenGapBetween`, `screenGapBottom`,
  `screenGapLeft`, `screenGapRight`, `screenGapTop`) and all 35 named
  `Krohnkite*` shortcut entries remain outside that minimal authorization and
  must not be changed without an explicit expanded cleanup request. Follow with
  a new read-only reconciliation.

## 2026-08-10 (Krohnkite authorized cleanup and reconciliation)

- Role / model: Lead (`lead-openai`, `openai/gpt-5.6-terra`); host-only work,
  no Worker dispatched.
- Authorized mutation: ran exactly `kwriteconfig6 --file kwinrc --group Plugins
  --key krohnkiteEnabled false`. The command exited successfully with no output.
  No other configuration, script, package, window, desktop, shortcut, service,
  or process mutation was performed.
- Read-only verification: `kreadconfig6` returns `false` for
  `[Plugins] krohnkiteEnabled`; `isScriptLoaded("krohnkite")` is `b false`; and
  KWin `org.freedesktop.DBus.Peer.Ping` succeeds. The proof id
  `plasma-auto-tiler-structural-proof` is `b false`; desktop vector, current,
  count, and rows each equal the durable pre-run snapshot; the attempted
  `[Tiling][099828b1-f155-487c-8801-42340019fb64]` subgroup is absent; no proof
  sentinel action exists in any KGlobalAccel component; and neither the proof
  sentinel prefix nor proof id occurs in `kglobalshortcutsrc`.
- Corrected process verification: self-excluding `pgrep -af` patterns report no
  `xterm -class PlasmaAutoTilerTestWindow` or matching Konsole sentinel process,
  and no LogSink `dbus-monitor`, FIFO demux, `run-proof.sh`, or
  `structural-proof.js` process. This supersedes the prior inconclusive process
  check.
- Remaining residue / scope: `[Script-krohnkite]` and 35 `Krohnkite*`
  `kglobalshortcutsrc` entries remain deliberately untouched. Krohnkite is
  disabled and not loaded. The failed `unit-04/attempt-01` runtime evidence
  remains inadmissible; no proof is resumed, no plan/spec change occurs, and no
  commit is made.
- Next safe action: return the failed unit status to the Orchestrator for a new
  scope/authorization decision. This cleanup does not authorize another unit-04
  run.

## 2026-08-10 (unit-04/attempt-02 staged protocol revision)

- Role / unit: Worker / `unit-04/attempt-02` (fresh bounded Worker, brief
  revision and staged-plan update only; no execution).
- Result: revised `research/proof-protocol.md` into an exact staged state
  machine and rewrote `proof/run-proof.sh` as a stage dispatcher
  (`run-proof.sh --stage PRE|AUT-KEY|AUT-WAY|AUT-BRANCH|M1|M2`). The revision
  removes the attempt-01 failure mode (a single persistent harness waiting on a
  go-signal file): every stage is one separate supervisor command bounded to a
  fixed 60-90 s deadline under `timeout --signal=INT --kill-after=5`, with an
  internal absolute-deadline check and INT/TERM/EXIT traps running the full
  per-stage reversal. No session persists across user turns; no go files, chat
  signals, FIFO/manual signals, open-ended polling, background wait, or
  indefinite wait. Manual stages M1 (drag-to-split, pass evidence
  `drag-finished,action=split`) and M2 (drag+Esc, pass evidence
  `drag-cancel-inferred`) are separate with separate evidence attribution and
  verbatim user announcements carrying a run-specific window title
  (`PLASMA-PROOF-WINDOW-A-<runid>`). Fresh proof/snapshot/evidence identifiers
  per repeated setup; Krohnkite not-loaded AND disabled preconditions and
  real-window exclusion enforced per stage; attempt-01 and PRE evidence remain
  inadmissible to proof success.
- Files / commit: rewritten `research/proof-protocol.md` and
  `proof/run-proof.sh`; updated `plan.md` (attempt-01 failed/inadmissible,
  attempt-02 authorized only under the staged protocol plus fresh user
  live-stage authorization, staged evidence map) and `state.md`; appended this
  `log.md`; `proof/structural-proof.js` and all `results/` files untouched; no
  commit.
- Verification: static, non-live checks only - `bash -n` on
  `proof/run-proof.sh`, `node --check` on `proof/structural-proof.js`, and a
  markdown section-structure sanity check on the protocol. No command exercised
  proof, harness, session, D-Bus, or config behavior.
- Notes / blocker: `unit-04/attempt-02` is NOT authorized and NOT executed; no
  proof completion is claimed. Fresh user authorization of the staged protocol
  plus a fresh user live-stage authorization before each manual stage is
  required before any stage runs. attempt-01 runtime evidence remains
  inadmissible.

## 2026-08-10 (unit-04 nested-KWin prerequisite approved)

- Role / unit: Lead / `unit-04/nested-kwin-feasibility`; user-approved plan and
  status correction.
- Result: further investment in the unsafe live-session harness is parked. One
  source-only, non-launching nested-KWin feasibility spike replaces it as the
  prerequisite to any future unit-04 live work. The spike may recommend one
  short smoke launch only if the source and installed-command evidence makes
  an isolated, deterministic, two-output script-and-client path obvious.
- Files / commit: updated `plan.md`, `state.md`, and `log.md`; no proof,
  runtime, source, dependency, configuration, or package artifact was modified;
  no commit.
- Verification: reconciled the approved spec, active plan/state/log, and the
  latest blocked attempt-01 artifacts. `results/progress.log` records
  `manual-t2 go-signal timeout`, `ABORTING`, and `RECOVERY_COMPLETE`; all prior
  runtime artifacts remain preserved and inadmissible.
- Next dispatch: exactly one fresh bounded Worker for source and non-launching
  installed-command capability inspection, writing one concise durable research
  file. No compositor, nested session, live-session query, proof harness,
  package installation, configuration edit, or source edit is authorized.

## 2026-08-10 (unit-04 nested-KWin prerequisite blocked)

- Role / unit: Lead / `unit-04/nested-kwin-feasibility`; one fresh Worker
  attempt, capped at 15 calls.
- Result: blocked the nested-KWin smoke path and recommended continuing without
  nested-environment infrastructure. The Worker stopped at 14 of 15 calls
  before it could write and verify its research file. It reported one scope
  deviation - reading inherited XDG/Wayland environment values - which is not
  evidence. No compositor, session, client, proof, installation, configuration,
  source, or live D-Bus/window/process query was run.
- Files / commit: added `research/nested-kwin-feasibility.md`; updated
  `plan.md`, `state.md`, and `log.md`; prior `proof/` and `results/` artifacts
  remain untouched; no commit.
- Verification: Lead inspected pinned Git objects at
  `45ec9a6d0ed312a803ff5658a2a3e61f221566c6` and non-launching
  `kwin_wayland --help`, `dbus-run-session --help`, and command-presence output.
  They establish nested/virtual flags, two startup virtual outputs,
  session-bus ownership, and installed launch prerequisites, but not the exact
  fully private runtime/socket or service composition required for safe reuse.
- Risks / next action: private runtime parent-socket access, isolated D-Bus and
  KGlobalAccel behavior, deterministic teardown, and external hotplug control
  remain unproven. No smoke launch is recommended or authorized; continue the
  production vertical slice without this infrastructure.

## 2026-08-10 (production-slice coordination)

- Role / model / unit: Lead (`lead-openai`, `openai/gpt-5.6-terra`) / cross-change plan coordination; no Worker dispatched.
- Result: recorded the user-approved `../custom-tile-vertical-slice/` Standard change as the owner of useful runtime discovery for its narrow production slice, through a separately authorized smoke only.
- Files / commit: updated this `plan.md`, `state.md`, and `log.md`, plus the active backlog; no research, proof, result, source, dependency, configuration, package, runtime, or live artifact changed; no commit.
- Verification: this change's unsafe live harness and nested-KWin path remain blocked. The production slice neither reuses nor unblocks them, and its future smoke cannot satisfy this change's unit-05 feasibility verdict.
- Next action: no feasibility live action is authorized. The production slice's next implementation unit is `unit-01/attempt-01`.
