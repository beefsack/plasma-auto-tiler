# COSMIC Controller-Path Directional Movement Log

## 2026-08-25

- Role / unit: Lead / planning / initial
- Result: User approved the successor specification and staged plan; Orchestrator approved the Expanded semantic plan.
- Files / commit: `spec.md`, `plan.md`, `log.md`, `state.md`; no commit.
- Verification: Approved path, units, contract, gates, review triggers, VCS policy, and separately gated host protocol recorded.
- Notes: The original parked COSMIC change remains untouched. A prior read-only Worker opened excluded pure-model material; its pure-model content is discarded. The breach is recorded as a planning-scope violation only and consumes no successor implementation, correction, review, or reset counter.
- Change-wide telemetry: implementation dispatches 0; dispatch-invalids 0; pre-review corrections 0; finding-fix corrections 0; independent reviews 0; changed-kind resets 0; broad gates 0; acceptance criteria moved 0; no-progress streak 0.

## 2026-08-25

- Role / unit: Lead / baseline reconciliation / read-only baseline-01
- Result: Accepted read-only baseline evidence; no implementation unit dispatched.
- Files / commit: `plan.md`, `log.md`, `state.md`; no commit.
- Verification: `npm --prefix kwin run typecheck` clean; `npm --prefix kwin test` 994 tests in 95 suites with 0 failures/skips; `bash scripts/start-test.test.sh` 255 assertions with 0 failures; keyboard lock SHA-256 `2ec613d2c9ed7b8fc3c00981b6ee1e3e67ee88bf34ff6ac3169eb21ba99a2dc7`.
- Notes: The Worker changed no files and performed no prohibited action. All current baselines are clean; `unit-01-fixture-corpus` remains not dispatched.
- Change-wide telemetry: implementation dispatches 0; dispatch-invalids 0; pre-review corrections 0; finding-fix corrections 0; independent reviews 0; changed-kind resets 0; broad gates 1; baseline Worker command proxy 4; acceptance criteria moved 0; no-progress streak 0.

## 2026-08-25

- Role / unit: Lead / pre-start semantic correction / fixture split
- Result: User approved and Orchestrator approved the fixture-support/controller-path split before any implementation dispatch.
- Files / commit: `spec.md`, `plan.md`, `log.md`, `state.md`; no commit.
- Verification: Unit 01 now accepts only isolated recursive mutable native-like fixture support, decode/snapshot oracle, ambiguity/failure primitives, and fixture self-tests. Unit 02 retains the adapter and owns all controller-started registered-shortcut directional conformance and production-operation recovery assertions.
- Notes: Overall scope, non-goals, hard invariants, dependencies, downstream gates, and blocked host protocol semantics are unchanged. No changed-kind reset, implementation attempt, correction, or review counter is consumed.
- Change-wide telemetry: implementation dispatches 0; dispatch-invalids 0; pre-review corrections 0; finding-fix corrections 0; independent reviews 0; changed-kind resets 0; broad gates 1; acceptance criteria moved 0; no-progress streak 0.

## 2026-08-25

- Role / unit: Lead / `unit-01-fixture-corpus` / attempt-01 and pre-review correction
- Result: Lead technically accepted the fixture-support implementation after one same-scope correction added the missing false-restoration rejection assertion. Mandatory independent review is pending.
- Files / commit: `kwin/tests/controller-fixtures.ts`, `kwin/tests/controller-cosmic-directional-movement.test.ts`, `plan.md`, `log.md`, `state.md`; no commit.
- Verification: G-01 clean; G-02 passed 6 fixture self-tests with 0 failures and 0 skips; G-03 matched `2ec613d2c9ed7b8fc3c00981b6ee1e3e67ee88bf34ff6ac3169eb21ba99a2dc7`. The same results were reported after the correction. Lead inspection confirmed fresh fixture ownership, recursive mutation, ambiguity/failure primitives, fresh decode, exact restoration and false-restoration rejection, and no controller/public-route or production-runtime use.
- Notes: The first attempt did not independently demonstrate that a mismatched snapshot fails closed, so the single permitted pre-review correction added that assertion. No production, controller, adapter, runtime, catalog/profile, host, historical/pure-model, staging, commit, or push action occurred. Worker tool-call proxies were not reported. The Lead scheduling threshold was reached before independent review dispatch.
- Change-wide telemetry: implementation dispatches 1; dispatch-invalids 0; pre-review corrections 1; finding-fix corrections 0; independent reviews 0; changed-kind resets 0; broad gates 1; acceptance criteria moved 1; no-progress streak 0.

## 2026-08-25

- Role / unit: Successor Lead / `unit-01-fixture-corpus` / mandatory independent review
- Result: Rejected pending one bounded finding-fix correction; the independent reviewer returned four confirmed fixture-contract findings.
- Files / commit: `plan.md`, `log.md`, `state.md`; no commit.
- Verification: No gates rerun. G-01 clean, G-02 6/6 with 0 failures/skips, and G-03 expected SHA-256 remain supplied evidence. Lead reconciliation confirmed: `unassigned` is excluded from decode status; recursive child cycles lack a guard; fixture windows retain shared mutable output/desktop/geometry defaults; and root-parent validity is unchecked.
- Notes: The correction must remain within the approved Unit 01 fixture files and add contract tests for each finding. No controller, adapter, production runtime, public route, host, historical/pure-model, staging, commit, push, archive, or unrelated-path action is authorized.
- Change-wide telemetry: implementation dispatches 1; dispatch-invalids 0; pre-review corrections 1; finding-fix corrections 0; independent reviews 1; changed-kind resets 0; broad gates 1; Worker tool-call proxies 4 plus Unit 01 Workers unreported; Lead tool-call proxy 43; acceptance criteria moved 1; no-progress streak 0.

## 2026-08-25

- Role / unit: Successor Lead / `unit-01-fixture-corpus` / one authorized finding-fix correction
- Result: Unaccepted and escalated. The correction is consumed; confirmation was not dispatched.
- Files / commit: `kwin/tests/controller-fixtures.ts`, `kwin/tests/controller-cosmic-directional-movement.test.ts`, `plan.md`, `log.md`, `state.md`; no commit.
- Verification: The Worker reported G-01 clean, G-02 10/10 with 0 failures/skips, and G-03 expected SHA-256. Lead inspection confirmed the four code repairs but found the new unassigned-window test calls `window()` at `controller-cosmic-directional-movement.test.ts:223` without importing it. The claimed G-02 result therefore cannot establish the focused test and technical acceptance is denied.
- Notes: This is a correction-introduced focused-test regression. The one allowed finding-fix correction is consumed; no confirmation, second correction, Unit 02, staging, commit, push, archive, downstream gate, or live work may start before parent escalation resolves the unit.
- Change-wide telemetry: implementation dispatches 1; dispatch-invalids 0; pre-review corrections 1; finding-fix corrections 1; independent reviews 1; changed-kind resets 0; broad gates 1; Worker tool-call proxies 4 plus Unit 01 Workers unreported; Lead tool-call proxy 46; acceptance criteria moved 1; no-progress streak 0.

## 2026-08-25

- Role / unit: Lead / `unit-01-fixture-corpus` / parking transaction
- Result: User selected parking under the consumed finding-fix breaker. Unit 01 is unaccepted; Unit 02 and downstream units are blocked.
- Files / commit: preserved `unit-01-candidate.patch`; restored `kwin/tests/controller-fixtures.ts`; updated `plan.md`, `log.md`, `state.md`, and `docs/backlog.md`; no commit.
- Verification: The preservation patch contains only `kwin/tests/controller-fixtures.ts` (361 additions, 3 deletions), SHA-256 `c33bf28f863ad53d325e7a19a003323443e0c8b2a4c865d88badb1618c0fb713`. After fixture restoration, `git apply --check` accepted the patch and the active fixture source had no diff against `HEAD`.
- Notes: The test was then incorrectly recorded as repository baseline and omitted from the container. That omission is corrected in the subsequent preservation-manifest record. Counters are frozen and no reset is consumed.
- Change-wide telemetry: implementation dispatches 1; dispatch-invalids 0; pre-review corrections 1; finding-fix corrections 1; independent reviews 1; changed-kind resets 0; broad gates 1; Worker tool-call proxies 4 plus Unit 01 Workers unreported; Lead tool-call proxy 46; acceptance criteria moved 1; no-progress streak 0.

## 2026-08-25

- Role / unit: Successor Lead / `unit-01-fixture-corpus` / preservation-manifest correction
- Result: Corrected the sole parking container to preserve both Unit 01 implementation paths; active source cleanup is complete and Unit 01 remains parked and unaccepted.
- Files / commit: amended `unit-01-candidate.patch`; deleted untracked `kwin/tests/controller-cosmic-directional-movement.test.ts`; updated `plan.md`, `log.md`, and `state.md`; no commit.
- Verification: `git apply --numstat` reports only `kwin/tests/controller-fixtures.ts` (361 additions, 3 deletions) and `kwin/tests/controller-cosmic-directional-movement.test.ts` (266 additions, 0 deletions). In a disposable `HEAD` worktree at `6466c99`, the test was absent, the fixture had no diff against `HEAD`, and `git apply --check` accepted the combined container. SHA-256: `dbe28416517a1966ddf3d8a18e3a01700b83ab3c6bd6d183d4c8988d4c15a434`.
- Notes: The prior manifest wrongly treated the untracked test as repository baseline, omitting it from preservation and explaining pointer-resize R-02's seven errors. The test is now preserved as a `/dev/null` creation diff and then deleted under the existing source-cleanup authorization. Lead / processed-beef process owns retention until an explicit changed-kind reset or container-cleanup approval. Pointer R-02 may retry its preservation check against this corrected container; no source implementation action is authorized.
- Change-wide telemetry: frozen - implementation dispatches 1; dispatch-invalids 0; pre-review corrections 1; finding-fix corrections 1; independent reviews 1; changed-kind resets 0; broad gates 1; Worker tool-call proxies 4 plus Unit 01 Workers unreported; Lead tool-call proxy 46; acceptance criteria moved 1; no-progress streak 0.

## 2026-08-27

- Role / unit: Lead / changed-kind reset / `unit-01-cosmic-local-fixture`
- Result: Reconciled and applied the user-authorized, Orchestrator-approved single reset. The blocked shared-fixture unit is retired unaccepted and replaced by an independently accepted COSMIC-local fixture/oracle unit in two new paths.
- Files / commit: `spec.md`, `plan.md`, `log.md`, `state.md`; no commit.
- Verification: Candidate SHA-256 observed as `dbe28416517a1966ddf3d8a18e3a01700b83ab3c6bd6d183d4c8988d4c15a434` without reading its content. G-01 through G-04, isolated worktree/source snapshots, focused/typecheck/broad baselines, and mandatory independent review are mapped in the amended plan.
- Notes: `unit-01-candidate.patch` is non-authoritative unaccepted audit/reference material. It remains byte-for-byte retained and must never be read, applied, copied, altered, relabeled, or used as acceptance evidence. The reset does not modify or import ownership from `kwin/tests/controller-fixtures.ts`; it permits only new `kwin/tests/cosmic-fixture.ts` and `kwin/tests/cosmic-fixture-contract.test.ts`. Active pointer source/tests/container, workspace-floor source/archive, tray/layout/install records, backlog, the candidate, and unrelated untracked paths are protected. Unit 02 and downstream units, pointer-owned paths, and host/live work remain stopped.
- Change-wide telemetry: implementation dispatches 1; dispatch-invalids 0; pre-review corrections 1; finding-fix corrections 1; independent reviews 1; changed-kind resets 1; broad gates 1; Worker tool-call proxies 4 plus Unit 01 Workers unreported; Lead tool-call proxy 65; acceptance criteria moved 1; no-progress streak 0.

## 2026-08-27

- Role / unit: Lead / `unit-01-cosmic-local-fixture` / attempt-01 dispatch
- Result: Valid fresh Worker semantic attempt dispatched to the detached clean snapshot.
- Files / commit: no source file changed by Lead; no commit.
- Verification: Pre-dispatch snapshot is `/tmp/opencode/cosmic-fixture-6466c99` at `6466c99dd497779d8499e0fef41cc5618593bff2`, with empty scoped status and root `kwin/node_modules` linked read-only under `/tmp/opencode`. Gate IDs, literal commands, baselines, allowed paths, and mandatory review trigger reconcile with the plan.
- Notes: The Worker may create or modify only `kwin/tests/cosmic-fixture.ts` and `kwin/tests/cosmic-fixture-contract.test.ts` in the isolated worktree; generated output is limited to `/tmp/opencode`. It must not access the candidate or protected paths and must stop on any surprise.
- Change-wide telemetry: implementation dispatches 2; dispatch-invalids 0; pre-review corrections 1; finding-fix corrections 1; independent reviews 1; changed-kind resets 1; broad gates 1; Worker tool-call proxy 4 plus Unit 01 Workers unreported; Lead tool-call proxy 67; acceptance criteria moved 1; no-progress streak 1.

## 2026-08-27

- Role / unit: Lead / `unit-01-cosmic-local-fixture` / attempt-01 reconciliation
- Result: Parked unaccepted. The isolated implementation was not transferred to the shared workspace and no independent review was dispatched.
- Files / commit: isolated-only `kwin/tests/cosmic-fixture.ts` and `kwin/tests/cosmic-fixture-contract.test.ts` remain in `/tmp/opencode/cosmic-fixture-6466c99`; change artifacts updated; no commit.
- Verification: Worker-reported G-01 clean, G-02 8 passing tests, G-03 expected hash, and one G-04 broad checkpoint with 1002 passing tests and 255 lifecycle assertions. Lead inspection confirmed the detached `HEAD` revision, scoped untracked paths only, no shared-workspace COSMIC-local files, no diff in protected `kwin/tests/controller-fixtures.ts`, and candidate SHA-256 unchanged at `dbe28416517a1966ddf3d8a18e3a01700b83ab3c6bd6d183d4c8988d4c15a434`.
- Notes: The Worker correctly stopped after an import search read forbidden controller-test paths. Independently, `restore()` does not restore topology after `split()`, and the contract lacks split-then-restore exact-equality proof. These are significant scope and exact-restoration-invariant failures. No correction, review, Unit 02, pointer path, host/live operation, staging, commit, push, archive, or candidate action is authorized.
- Change-wide telemetry: implementation dispatches 2; dispatch-invalids 0; pre-review corrections 1; finding-fix corrections 1; independent reviews 1; changed-kind resets 1; broad gates 2; Worker tool-call proxy 4 plus Unit 01 Workers unreported; Lead tool-call proxy 72; acceptance criteria moved 1; no-progress streak 1.

## 2026-08-27

- Role / unit: Successor Lead / `unit-01-cosmic-local-fixture` / correction preflight
- Result: Handover-blocked before Worker dispatch. No correction, review, promotion, source edit, or gate run occurred.
- Files / commit: created fresh detached `/tmp/opencode/cosmic-fixture-correction-6466c99` at `6466c99dd497779d8499e0fef41cc5618593bff2` with a managed `kwin/node_modules` symlink; updated `log.md` and `state.md`; no commit.
- Verification: The isolated worktree has the required base revision and empty scoped status. G-02 and G-04 literally remove and recreate the existing `/tmp/opencode/cosmic-fixture-g02` and `/tmp/opencode/cosmic-fixture-g04` directories, which conflicts with preserving old temporary evidence. Private mount isolation is unavailable (`unshare --mount` is not permitted). The root managed `kwin/node_modules` is writable at the filesystem level, so the requested read-only dependency condition cannot be technically established by its symlink alone.
- Notes: A broad Lead reconciliation content query inadvertently included the protected candidate path. The candidate was not altered, applied, copied, transferred, or supplied to a Worker, but its returned content is ineligible for this reset. No Worker is dispatched until the parent resolves the contradictory gate-output and dependency-read-only requirements. Counters are unchanged.
- Change-wide telemetry: unchanged - implementation dispatches 2; dispatch-invalids 0; pre-review corrections 1; finding-fix corrections 1; independent reviews 1; changed-kind resets 1; broad gates 2; Worker tool-call proxy 4 plus Unit 01 Workers unreported; Lead tool-call proxy 72; acceptance criteria moved 1; no-progress streak 1.

## 2026-08-27

- Role / unit: Successor Lead / `unit-01-cosmic-local-fixture` / verification-only gate amendment
- Result: Recorded the Orchestrator-approved autonomous amendment before correction dispatch. G-01 through G-04 now use the fresh nonce-owned `/tmp/opencode/cosmic-fixture-correction-6466c99-20260826T144407Z` attempt directory rather than the preservation-protected shared gate-output paths.
- Files / commit: `plan.md`, `log.md`, `state.md`; no source file changed and no commit.
- Verification: The amendment preserves every gate baseline, acceptance criterion, source constraint, and dependency. It binds the fresh isolated worktree, output, and npm-cache paths and prohibits dependency installation/update and all writes to managed `node_modules`.
- Notes: Commands may create, overwrite, or delete only within the new attempt directory. Existing evidence directories, old failed temporary source/evidence, the candidate, controller fixtures/tests, pointer paths, and unrelated work remain unread and protected.
- Change-wide telemetry: unchanged - implementation dispatches 2; dispatch-invalids 0; pre-review corrections 1; finding-fix corrections 1; independent reviews 1; changed-kind resets 1; broad gates 2; Worker tool-call proxy 4 plus Unit 01 Workers unreported; Lead tool-call proxy 77; acceptance criteria moved 1; no-progress streak 1.

## 2026-08-27

- Role / unit: Successor Lead / `unit-01-cosmic-local-fixture` / ordinary pre-review correction-01 and source promotion
- Result: The one authorized correction is review-ready and was promoted byte-for-byte to the shared workspace in only the two approved COSMIC-local paths. Mandatory independent review is pending; this is not acceptance.
- Files / commit: added `kwin/tests/cosmic-fixture.ts` and `kwin/tests/cosmic-fixture-contract.test.ts`; updated `plan.md`, `log.md`, and `state.md`; no commit.
- Verification: Fresh detached `6466c99dd497779d8499e0fef41cc5618593bff2` snapshot; scoped isolated diff and shared promotion contain only the two approved untracked paths; `git diff --check` clean; byte-for-byte `cmp` matches. Amended G-01 clean; G-02 8 tests with 0 failures/skips; G-03 expected SHA-256; G-04 1002 tests and 255 lifecycle assertions with 0 failures. The contract contains split-then-restore fresh-decoded exact equality plus restoration-write failure preservation.
- Notes: Existing evidence directories, old temporary source/evidence, candidate, controller fixtures/tests, pointer paths, and unrelated work were neither read nor modified. The correction budget is now consumed. No second pre-review correction may be dispatched.
- Change-wide telemetry: implementation dispatches 2; dispatch-invalids 0; pre-review corrections 2; finding-fix corrections 1; independent reviews 1; changed-kind resets 1; broad gates 3; Worker tool-call proxy 4 plus Unit 01 Workers unreported; Lead tool-call proxy 89; acceptance criteria moved 1; no-progress streak 1.

## 2026-08-27

- Role / unit: Successor Lead / `unit-01-cosmic-local-fixture` / mandatory independent review
- Result: Rejected pending the one permitted finding-fix correction for frozen finding set F-01/F-02. No source changed during review.
- Files / commit: `plan.md`, `log.md`, and `state.md`; no commit.
- Verification: Review inspected only the promoted two paths and their scoped VCS provenance. It confirmed valid-snapshot split-then-restore fresh-decoded topology equality, then found `parseSnapshot()` accepts a non-leaf window assignment and `parseRect()` accepts non-finite numeric values. Both can reach `restore()` and violate fail-closed handling.
- Notes: The finding fix is confined to rejecting F-01/F-02 and adding their focused contract assertions. It is not a second pre-review correction. No candidate, old evidence, controller fixture/test, pointer path, unrelated source, or host path was inspected.
- Change-wide telemetry: implementation dispatches 2; dispatch-invalids 0; pre-review corrections 2; finding-fix corrections 1; independent reviews 2; changed-kind resets 1; broad gates 3; Worker tool-call proxy 4 plus Unit 01 Workers unreported; Lead tool-call proxy 93; acceptance criteria moved 1; no-progress streak 1.

## 2026-08-27

- Role / unit: Successor Lead / `unit-01-cosmic-local-fixture` / frozen finding-fix correction and acceptance confirmation
- Result: Accepted. F-01 and F-02 are closed without scope widening, a second pre-review correction, or a second independent review.
- Files / commit: updated only `kwin/tests/cosmic-fixture.ts` and `kwin/tests/cosmic-fixture-contract.test.ts` for source; updated `plan.md`, `log.md`, and `state.md`; no commit.
- Verification: The finding fix rejects non-leaf window assignments and non-finite geometry before topology rebuild and adds focused assertions. Amended G-01 clean; G-02 9 tests with 0 failures/skips; G-03 expected SHA-256; G-04 1003 tests and 255 lifecycle assertions with 0 failures. Lead confirmation verified the two promoted files are byte-identical to the gated isolated worktree and `git diff --check` is clean.
- Notes: F-01/F-02 is the sole independent-review finding set and is closed. The nonce-owned attempt directory retains evidence; no cleanup, staging, commit, push, archive, live operation, or Unit 02 work occurred.
- Change-wide telemetry: implementation dispatches 2; dispatch-invalids 0; pre-review corrections 2; finding-fix corrections 2; independent reviews 2; changed-kind resets 1; broad gates 4; Worker tool-call proxy 4 plus Unit 01 Workers unreported; Lead tool-call proxy 98; acceptance criteria moved 2; no-progress streak 0.

## 2026-08-27

- Role / unit: Successor Lead / `unit-02-controller-adapter` / dependency and dispatch preflight
- Result: `dispatch-invalid` before source work. The accepted Unit 01 fixture dependency is satisfied, but Unit 02 has no approved exact allowed paths, literal controller-path commands, or expected baselines.
- Files / commit: `plan.md`, `log.md`, and `state.md`; no source file changed and no commit.
- Verification: Host reconciliation found base `6466c99dd497779d8499e0fef41cc5618593bff2`; active accepted workspace-floor source diff is in `kwin/src/controller.ts`; accepted COSMIC-local inputs are `kwin/tests/cosmic-fixture.ts` and `kwin/tests/cosmic-fixture-contract.test.ts`. A later isolated snapshot must bind those accepted inputs plus only the Unit 02 candidate diff. `worker-openai`, task dispatch, and the Worker skill are available; the plan-to-brief gate/path comparison fails.
- Notes: No candidate content was read or changed. No Unit 02 Worker was dispatched, no source or temporary snapshot was created, and no gate, pointer, controller-fixtures/test, host/live, staging, commit, push, archive, workspace-floor, layout, tray, install, backlog, or unrelated-path action occurred. Supplying the missing scope and gates is a semantic plan amendment requiring Orchestrator approval, not a repairable dispatch-packet omission.
- Change-wide telemetry: implementation dispatches 2; dispatch-invalids 1; pre-review corrections 2; finding-fix corrections 2; independent reviews 2; changed-kind resets 1; broad gates 4; Worker tool-call proxy 4 plus Unit 01 Workers unreported; Lead tool-call proxy 122; acceptance criteria moved 2; no-progress streak 0.

## 2026-08-27

- Role / unit: Lead / `unit-02-controller-adapter` / approved semantic amendment
- Result: Recorded the Orchestrator's autonomous approval of exact Unit 02 paths, snapshot boundary, literal static gates, and baselines. The prior preflight remains one `dispatch-invalid`, not a semantic attempt or correction.
- Files / commit: `spec.md`, `plan.md`, `log.md`, and `state.md`; no source file changed and no commit.
- Verification: The amendment permits only controller composition, one new narrow controller adapter, and one new controller-path test. It binds a fresh nonce-owned output path, a read-only-use dependency symlink, shell-expanded test inputs, directory-based test execution, workspace-floor preservation, and mandatory independent review.
- Notes: Unit 03 remains blocked. Candidate, pointer, fixture ownership, host/live, staging, commit, push, archive, and unrelated paths remain protected.
- Change-wide telemetry: unchanged - implementation dispatches 2; dispatch-invalids 1; pre-review corrections 2; finding-fix corrections 2; independent reviews 2; changed-kind resets 1; broad gates 4; Worker tool-call proxy 4 plus Unit 01 Workers unreported; Lead tool-call proxy 122; acceptance criteria moved 2; no-progress streak 0.

## 2026-08-27

- Role / unit: Lead / documentation reconciliation
- Result: Unit 01 remains historically accepted, but its fixture files are untracked and uncommitted; its current promotion is unaccepted because G-04 has no fresh passing evidence.
- Files / commit: `docs/backlog.md`, `plan.md`, `log.md`, and `state.md`; documentation commit pending.
- Verification: Historical G-04 evidence remains distinct from current promotion evidence. The canonical G-04 output contract omitted package-relative paths and tray environment, so it cannot establish current freshness.
- Notes: The sole changed-kind reset is consumed. No second review, correction, reset, source action, candidate action, gate run, or Unit 02 continuation is authorized. Unit 02 remains parked pending whether locked legacy directional behavior is product-required and a separately approved runtime-capability/migration direction.
- Change-wide telemetry: unchanged - implementation dispatches 3; dispatch-invalids 1; pre-review corrections 2; finding-fix corrections 2; independent reviews 2; changed-kind resets 1; broad gates 5; Worker tool-call proxy 4 plus Unit 01 Workers unreported plus Unit 02 attempt-01 unreported; Lead tool-call proxy 122; acceptance criteria moved 2 plus canonical-gate advances G-02-01/G-02-02/G-02-03; no-progress streak 0.

## Current Authorization - 2026-08-30

- One isolated current Unit 01 promotion refresh is authorized. It may correct
  only G-04 package-relative paths and tray environment. The sequence is G-01,
  G-02, G-03, G-04, then independent source/output correspondence review;
  failure parks the promotion.
- Unit 02 may carry `directionalMovement` through production controller
  composition and tests only as necessary. COSMIC replaces legacy directional
  movement with no fallback, and legacy expectations migrate to COSMIC.
- The old candidate remains prohibited from reading, use, promotion,
  relabelling, or acceptance evidence. No unrelated source or live work is
  authorized.

## Current Reconciliation - 2026-08-30

- This authorization supersedes only the prior Unit 02 product/capability
  blocker. COSMIC replaces legacy directional behavior; `directionalMovement`
  may reach production controller composition and corresponding tests only as
  necessary, with no legacy fallback or dual route. The historical attempt and
  old candidate remain unaccepted and protected.

## 2026-08-27

- Role / unit: Successor Lead / `unit-02-controller-adapter` / attempt-01 reconciliation
- Result: Paused for an Orchestrator scope and canonical-gate decision. The isolated candidate is not promoted and mandatory independent review is not dispatched.
- Files / commit: isolated-only `kwin/src/controller.ts`, `kwin/src/controller-directional-movement.ts`, and `kwin/tests/controller-cosmic-directional-movement.test.ts`; updated `plan.md`, `log.md`, and `state.md`; no commit.
- Verification: `S-02-pre` is `/tmp/opencode/cosmic-controller-adapter-6466c99-20260826T213005Z-68811/worktree` at `6466c99dd497779d8499e0fef41cc5618593bff2`. The protected workspace-floor hunk and accepted fixture inputs remain byte-identical. G-02-01 passed 5 named tests with zero failures/skips at `output/g02-01/controller-cosmic-directional-movement.test.js`; G-02-02 exited 0; G-02-03 matched the locked SHA-256. G-02-04 bundled to `output/g02-04/tests`, but its canonical directory invocation failed under the current Node runner; an expanded-file diagnostic then exposed frozen legacy directional-controller failures because their environments lack the new adapter runtime capability.
- Notes: The isolated Unit 02 changes replace the legacy controller delegate with a closed adapter route, add the adapter, and add controller-started canonical/arrow conformance coverage. The runtime capability is not available to existing controller environments. Adding legacy input-action behavior is a forbidden fallback; providing the capability requires excluded fixture or integration changes. No source diff is accepted, no recovery/restoration claim is accepted, and no gate result is inferred from the diagnostic. Candidate, fixture, pointer, workspace-floor, tray/layout/install/backlog, live, staging, commit, push, archive, and unrelated paths remain untouched.
- Change-wide telemetry: implementation dispatches 3; dispatch-invalids 1; pre-review corrections 2; finding-fix corrections 2; independent reviews 2; changed-kind resets 1; broad gates 5; Worker tool-call proxy 4 plus Unit 01 Workers unreported plus Unit 02 attempt-01 unreported; Lead tool-call proxy 122; acceptance criteria moved 2 plus canonical-gate advances G-02-01/G-02-02/G-02-03; no-progress streak 0.

## 2026-08-27

- Role / unit: Lead / `unit-02-controller-adapter` / gate correction and parking
- Result: Orchestrator-approved G-02-04 runner correction recorded. Unit 02 is parked; no source, gate, or review action occurred.
- Files / commit: `plan.md`, `log.md`, and `state.md`; no source file changed and no commit.
- Verification: The corrected future command is `node --test "$ATTEMPT/output/g02-04/tests"/*.test.js`, retaining the same nonce-owned esbuild output directory and full bundled-test coverage. No command was run under this correction.
- Notes: Attempt-01 at `/tmp/opencode/cosmic-controller-adapter-6466c99-20260826T213005Z-68811/worktree` remains unaccepted, ineligible isolated evidence and is not promoted. The only activation condition is a user decision on whether locked legacy directional behavior is product-required and then a separately approved capability boundary. No capability prerequisite, fallback, dual routing, locked-test change, or product-semantic decision is authorized. Unit 01 acceptance, all counters, protected artifacts, and Unit 03's dependency block remain unchanged.
- Change-wide telemetry: unchanged - implementation dispatches 3; dispatch-invalids 1; pre-review corrections 2; finding-fix corrections 2; independent reviews 2; changed-kind resets 1; broad gates 5; Worker tool-call proxy 4 plus Unit 01 Workers unreported plus Unit 02 attempt-01 unreported; Lead tool-call proxy 122; acceptance criteria moved 2 plus canonical-gate advances G-02-01/G-02-02/G-02-03; no-progress streak 0.

## 2026-08-27

- Role / unit: Successor Lead / `unit-02-controller-adapter` / fresh snapshot and packet preflight
- Result: Valid snapshot and dispatch packet prepared for one fresh semantic attempt.
- Files / commit: `plan.md`, `log.md`, and `state.md`; no source file changed and no commit.
- Verification: `S-02-pre` is `/tmp/opencode/cosmic-controller-adapter-6466c99-20260826T213005Z-68811/worktree` detached at `6466c99dd497779d8499e0fef41cc5618593bff2`; only the accepted workspace-floor controller hunk and accepted COSMIC fixture/oracle inputs are present, each byte-identical to the workspace. `kwin/node_modules` is a symlink to the verified repository-managed locked tree. The locked keyboard source SHA-256 matches `2ec613d2c9ed7b8fc3c00981b6ee1e3e67ee88bf34ff6ac3169eb21ba99a2dc7`.
- Notes: `worker-openai`, single task dispatch, and the direct source Worker skill are available. The Worker packet has exact allowed paths, dependency, canonical gates, static baselines, review trigger, and stop conditions. The prior `dispatch-invalid` remains pre-source and consumes no Unit 02 implementation, correction, or review budget. Candidate, active pointer work/container, workspace-floor source/archive, tray/layout/install/backlog changes, and unrelated untracked paths remain protected.
- Change-wide telemetry: unchanged - implementation dispatches 2; dispatch-invalids 1; pre-review corrections 2; finding-fix corrections 2; independent reviews 2; changed-kind resets 1; broad gates 4; Worker tool-call proxy 4 plus Unit 01 Workers unreported; Lead tool-call proxy 122; acceptance criteria moved 2; no-progress streak 0.
