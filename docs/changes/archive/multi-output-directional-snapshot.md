# Multi-output directional snapshot rejection

## Goal and scope

- Diagnose the multi-output directional `window-out-of-bounds` refusal and make carried multi-domain geometry use the member's own output bounds. Preserve safe planner validation and cross-output behavior; no live mutation by the implementation, commit or push.

## Evidence and outcome (2026-09-26)

- Original `~/Downloads/plasma-auto-tiler-dev.4lmVmo.log:389-427`: source-only p8 applies, then p9 (3 windows, 2 domains) rejects `snapshot-invalid/window-out-of-bounds`. `kwin/src/plan-adapter-entry.ts` combines source and target windows for horizontal commands; `crates/tiler-protocol/src/planner_protocol.rs:1149-1160` rejects any non-floating carried window outside its own raw output bounds before planning. The old log does not identify the offending rectangle. W3's foreground `output-mismatch` is normal per-output filtering; no output-2 geometry write occurs in that trace.
- The previous source-only fallback was removed. `kwin/src/plan-adapter.ts:2064-2082` now clamps overlay or previously applied rectangles against the member's *own* output bounds, instead of always using source bounds. Tests cover a target overlay. `rejectKind` names the offending output, ordinal, resource class, rect and own bounds without a native ID only on `snapshot-invalid/window-out-of-bounds`.
- The user reports focus/navigation and window movement working on both outputs in `~/Downloads/plasma-auto-tiler-dev.rRHFMS.log`. That trace has two-domain focus planned (`:148-150`) and cross-output move planned (`:1170-1172`), with HDMI-A-2's normal Ghostty frames already inside its `2048,116,1920,1036` bounds (`:43-45,1185-1186`). No `window-out-of-bounds` rejection occurs. It does **not** prove the earlier failure was caused by source-bound clamping: normal unknown/unapplied windows bypass that clamp entirely (`plan-adapter.ts:2072-2079`), and the old trace showed no W3 output-2 write. The trace also records a focus reply timeout (`:79`) and a cross-output move stale-scope terminal (`:1203`); subsequent actions remain usable. The precise original offending window and reason W3 was then untiled remain unproven; this successful live run is accepted for observed navigation/movement, not retrospective root-cause attribution.
- The temporary opt-in background diagnostics generated 209 per-window and 107 domain-decision lines in this 1608-line capture; removed as noisy. The rejection-only diagnostic remains. No durable product/architecture decision was made and `docs/decisions.md` is unchanged.
- Verification after cleanup: KWin `npm test --prefix kwin` 743/743, `npm run typecheck --prefix kwin`, and `git diff --check` pass. Rust source untouched (workspace tests passed before this KWin-only cleanup). No live-session mutation, commit or push by the Lead.
