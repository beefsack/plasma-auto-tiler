# Multi-output PC failures

## Goal and scope

Diagnose and correct the shortcut Apply refusal and rejected focus/move commands reported on a second multi-output PC. Offline only: no live KWin/Plasma testing, shortcut-config mutation, staging, or commits. Do not edit the backlog, principles, architecture review, or devenv.nix channel/targets.

## Delivery split and acceptance

- Delivery 1: align directional focus/move window containment with raw per-domain bounds, keep inset bounds for projection; add lean edge-tiled two-output regression coverage and preserve single-domain behavior. Run Rust workspace tests, fmt, strict clippy, `just check-portable`, `git diff --check`, and fresh independent Worker review; return for user commit and multi-output live test before continuing.
- Delivery 2, only after user resumes: simplify shortcut Force to confirmed clearance of every holder of a required key from a fresh read; Revert uses KDE defaults, and the preimage journal and migration are retired without removing old journal files. Preserve bounded shortcut diagnostics and avoid per-component `cleanUp()`. Update decisions and shortcut change note then run native/relevant shell checks and independent review.

## Approach

1. Fresh bounded read-only Worker: shortcut Apply/Force diagnosis with exact references.
2. Fresh bounded read-only Worker: trace and multi-output observation/bounds diagnosis with exact references.
3. Implement and verify Delivery 1, one fresh small Worker at a time; return for user commit and testing.
4. On user resumption implement Delivery 2, verify, review, and archive at completion.

## Accepted diagnosis (read-only, 2026-09-26)

- Trace `~/Downloads/plasma-auto-tiler-dev.caWaFc.log:4` says `source=local-dev generation=plan-1` without a git SHA; tested commit remains unconfirmed. Lines 5-6 show current float/maximize shortcuts shadowed by Grid View/Monocle, not the KCM Apply failure. No `plasmaautotiler.shortcut op=` records are in this trace.
- Shortcut Apply checks every holder on relevant keys against the closed exact table (`kwin/native-effect/shortcutreconciler.cpp:504-535,1149-1240`). Legacy `kwin/plasma-auto-tiler-float-toggle` existed historically (`0c089f1`); current action is `plasma-auto-tiler-toggle-float` (`kwin/native-effect/shortcutreconciler.h:151-158`). The reported `kwin/plasma-auto-tiler-toggle` also is not in the table. Journal migration copies journal images exactly, not stale global-accelerator action IDs (`shortcutreconciler.cpp:2447-2515`). Force runs the same occupancy preflight before offering only exact compiled foreign clear rows (`shortcutreconciler.cpp:3442-3492,3575-3603`); it cannot clear those reported own-action holders. The live holder images, journal state, exact tested SHA and whether Force was actually invoked are unproven without second-PC evidence.
- Independently verified trace counts: 87 `observe-excluded reason=output-mismatch`, 11 directional focus/move egress `window-out-of-bounds`, 6 focus/move egress `unchanged`, 20 `planned-applied`. First rejection: trace lines 73-87 (5 windows, 2 domains). Output mismatch excludes other-output windows by `output.name` as expected (`kwin/src/plan-adapter-entry.ts:1498-1557,1582-1590`); no evidence that these exclusions themselves cause rejection. Domain workspace is the per-output current desktop and bounds are quantized PlacementArea. Horizontal adjacency requires touching bounds with positive overlap (`plan-adapter-entry.ts:1875-1893`). The trace supports non-zero horizontal origin and differing workareas, not scale differences, vertical stacking, spanning windows, or duplicate names.
- Directional containment compares carried window rectangles to *inset* domain bounds (`crates/tiler-protocol/src/planner_protocol.rs:1121-1152`), unlike single-domain containment against raw workarea (`:1163-1181`). Trace edge-tiled rectangles near raw workarea edges explain directional rejection while same-domain operations succeed; the log lacks offending window/bounds/gap, so the exact failed rectangle remains unproven. Smallest correction is raw per-domain containment for directional commands, retaining inset bounds for projection. Add a lean real-Engine regression with two offset outputs and edge-tiled observed windows; cover observer homing/adjacency and narrow exclusion where practical. Report the failing stable window/domain with correlation without raw native IDs or payloads.

## User decision and state

User (2026-09-26) authorized a two-delivery approach. For Delivery 2, Force may clear ANY holder of a project-required key after showing exact holders and receiving confirmation, with a fresh live read immediately before applying. Revert restores defaults using KGlobalAccel `defaultShortcutKeys` and `setForeignShortcutKeys`; the user accepts loss of custom cleared bindings. Retire the exact-preimage journal/validation/migration; leave existing journal files untouched. These changes supersede the option analysis above and are intentionally deferred until the user resumes after Delivery 1. The approved direction is in the user's message and `docs/backlog.md` (maintained by the Orchestrator); update `docs/decisions.md` with accurate attribution in Delivery 2.

Delivery 1 completed, committed and pushed by the user as `8162377` (Rust-only). `crates/tiler-protocol/src/planner_protocol.rs:1121-1155` uses raw carried directional bounds and retains inset domains for projection. Real-Planner regression `:4039-4263` checks accepted focus/cross-output move, inset geometry and one-pixel raw out-of-bounds refusal. Original refusal reproduced before the fix. Rust workspace tests 609/609, format, strict clippy, portable check and diff check passed; independent review found no blocking issue. Second-PC live check is still user-owned.

## Delivery 2 working plan (resumed 2026-09-26)

- Backend: remove obsolete preimage-journal, exact allowlist, migration and interrupted-operation machinery. Retain only minimal durable IDs for actions cleared, if necessary for Revert across KCM/process restart, without touching legacy journal files. Keep current explicit confirmation, owner-pinned transport, strict parsing/identity bounds, fresh revalidation before writes, and bounded diagnostics. A foreign action with multiple shortcuts loses only the required chord when clearing; Revert replaces its full active set with `defaultShortcutKeys`, as user approved. Preserve project key assignments and Lock Session relocation where they are still required; never use component `cleanUp()`.
- UI: present every action/chord that Force will clear before confirmation, including unknown and legacy project actions. Delete obsolete Finish/Restore and journal-phase presentation. Preserve ordinary settings Save isolation and cancel/no-write behavior.
- Offline transport evidence: installed KF6 6.30 XML `/nix/store/9q48dz6yk4smw7xp1sqq6f5q8a627y8g-kglobalaccel-6.30.0/share/dbus-1/interfaces/kf6_org.kde.KGlobalAccel.xml:123-127,145-149` declares per-action `defaultShortcutKeys(as)->a(ai)` and `setForeignShortcutKeys(as,a(ai))->void`; the KGlobalAccelD 6.7.5 headers confirm foreign ownership. Test the exact wire contract in fakes; verify void writes via fresh readback. No live bus or host config has been touched.
- Bounded units, one fresh Worker at a time: transport seam and tests; backend state/clear/revert rewrite and regression tests; UI/dialog and tests; documentation; applicable checks; independent final-diff review. Stop at green points, recording blockers and exact next action here. No staging/commit/push.

### Green point and handover

Current branch `main` at `8162377`. Unit 1 (native transport only) has changed `kwin/native-effect/shortcutreconciler.{h,cpp}`, `kwin/native-effect/shortcutreconciler_test.cpp`, `kwin/native-effect/activeborderconfig_shortcut_test.cpp`, plus this note. It introduces injected `defaultShortcutKeys` and `setForeignShortcutKeys` methods, strict typed reply validation, owner-pinned D-Bus calls, void-setter fresh readback, hermetic fakes/transport tests. This is **provisional**: the transport still uses the old exact allowlist (`shortcutreconciler.cpp:2103,2170`) and does not count foreign writes (`:2169`); both must be changed before Force can clear arbitrary holders. The inherited journal/Force/UI behavior is unchanged so far. No shortcut journal file or live session has been touched.

Unit 1 verified offline: `just build-native-effect` succeeded; the exact host KWin 6.7.5 derivation dev shell configured BUILD_TESTING=ON and ran CTest 29/29, plus focused native shortcut malformed 1/1 (new transport decoder/fake test). `git diff --check` passed after this unit. No code has been staged/committed. These are interim results only; rerun all required checks after final changes.

**Design blocker (read-only, 2026-09-26):** The KF6 6.30 Component `allShortcutInfos` XML exposes both active and default keys (`a(ssssssaiai)`), but stateless Revert based on defaults would reset actions Force never cleared, potentially activate a default-only holder that was previously disabled, and miss custom holders whose default lacks the required key. A minimal durable list of cleared component/action IDs avoids those cases while surviving interrupted Force if persisted before clearing. The user explicitly requested options before choosing when stateless Revert has additional surprising behavior; no implementation or live mutation was performed in this investigation.

**Orchestrator decision (2026-09-26):** Choose the minimal persisted list of cleared component/action IDs, written before Force clearing, consumed idempotently by Revert and then emptied. Stateless Revert is rejected for the unexpected behavior above. Leave legacy journal files untouched. Next: fresh bounded design Worker for store seam and interruption sequencing; then small backend units, KCM UI/dialog/tests, docs, full checks, and independent final-diff review, one fresh `muse-spark` Worker at a time. No live KWin/Plasma mutation, staging, or commit.

**Transport green point:** Fresh Worker removed compiled allowlist gates on the pinned default/foreign transport, counted foreign writes in the lifetime cap, and covered arbitrary-ID round trips, readback and owner validation. `just build-native-effect`, native CTest 29/29 (shortcut subset 15/15), and `git diff --check` pass. Legacy journal and UI still present; backend rewrite is next.

**Backend and UI green point:** Minimal new `shortcut-clearedrc` store holds only cleared component/action IDs; Force previews all active holders and exact removals, re-reads before persistence, persists the union before clearing, preserves other keys; Revert resolves fresh action labels, restores KDE defaults for non-project cleared actions and empties the store only on success. The prior journal implementation, migration, and UI recovery controls are removed without accessing old files. KCM Force dialog and tests cover preview/cancel/confirm, normal Save isolation and cleared-list recovery. `just build-native-effect`, host-matched native CTest 29/29, `git diff --check` pass.

**Final offline evidence and outcome (2026-09-26):** Host-matched KWin 6.7.5 native build and CTest 29/29 pass after the ID-only store correction. Relevant shell suites: `scripts/dev-native-effect.test.sh` 163/0, `scripts/nix-host-kwin-build.test.sh` 93/0, `scripts/build-kpackage.test.sh` contract pass (its expected negative-case stderr is not a failure). `git diff --check` passes. No KWin TypeScript files were touched, so no KWin TS suite was required. Independent review found three actionable issues: known foreign holders showed Ready while Apply refused; a holder could drift after persistence and lose unrelated keys; Force button copy implied confirmation already happened. The fixes align the UI status exemption with backend Force, re-read each holder immediately before a foreign setter (on drift retain the ID superset and stop), and correct the button copy. The persisted list was further reduced to IDs only; Revert resolves current labels from a fresh tuple read and leaves the list for retry on absent or duplicate IDs. A fresh independent final-diff Worker review after these corrections found no blocking findings and confirmed net deletion vs HEAD. No live session mutation, staging or commit occurred. The remaining user-owned second-PC check is described in `docs/live-shortcut-override-verification.md`: fresh native build/staging and Plasma login, Apply/Force preview and Revert to defaults, physical shortcuts, old journals untouched. Orchestrator owns the backlog transition; exact next coding action: none.
